C = Class { .x = Nat }
C|C <: Add(Int)|.
    Output = Int
    __add__ self, other: Int =
        self.x + other
C|C <: Add(Nat)|.
    Output = Nat
    __add__ self, other: Nat =
        log "Nat"
        self.x + other

c = C.new { .x = 1 }
_: Int = c + -1
_: Nat = c + 1
