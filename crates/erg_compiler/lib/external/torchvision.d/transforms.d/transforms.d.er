Transform: ClassType

.Compose: ClassType
.Compose <: GenericCallable
.Compose.
    __new__: (cls: ClassType, transforms: [Transform; _]) -> .Compose

.CenterCrop: ClassType
.CenterCrop <: Transform
.CenterCrop <: GenericCallable
.CenterCrop.
    __new__: (cls: ClassType, size: Nat or (Nat, Nat) or [Nat; 2]) -> .CenterCrop

.GrayScale: ClassType
.GrayScale <: Transform
.GrayScale <: GenericCallable
.GrayScale.
    __new__: (cls: ClassType, ) -> .GrayScale

.Normalize: ClassType
.Normalize <: Transform
.Normalize <: GenericCallable
.Normalize.
    __new__: (
        cls: ClassType,
        mean: [Float; _],
        std: [Float; _],
        inplace := Bool,
    ) -> .Normalize

.RandomCrop: ClassType
.RandomCrop <: Transform
.RandomCrop <: GenericCallable
.RandomCrop.
    __new__: (
        cls: ClassType,
        size: Nat or (Nat, Nat) or [Nat; 2],
        padding := Nat or (Nat, Nat) or (Nat, Nat, Nat, Nat),
        pad_if_needed:=Bool,
        fill := Nat or (Nat, Nat, Nat),
        padding_mode := Str,
    ) -> .RandomCrop

.RandomHorizontalFlip: ClassType
.RandomHorizontalFlip <: Transform
.RandomHorizontalFlip <: GenericCallable
.RandomHorizontalFlip.
    __new__: (cls: ClassType, ) -> .RandomHorizontalFlip

.RandomResizedCrop: ClassType
.RandomResizedCrop <: Transform
.RandomResizedCrop <: GenericCallable
.RandomResizedCrop.
    __new__: (cls: ClassType, size: Nat or (Nat, Nat) or [Nat; 2]) -> .RandomResizedCrop

.Resize: ClassType
.Resize <: Transform
.Resize <: GenericCallable
.Resize.
    __new__: (
        cls: ClassType,
        size: Nat or (Nat, Nat) or [Nat; 2],
        interpolation := Str,
        max_size := Nat,
        antialias := Bool,
    ) -> .Resize

.ToTensor: ClassType
.ToTensor <: Transform
.ToTensor <: GenericCallable
.ToTensor.
    __new__: (cls: ClassType, ) -> .ToTensor
